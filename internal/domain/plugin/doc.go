// Package plugin is reserved for domain types and rules related to plugins.
// Currently, plugin entities (Manifest, HelpGroup, etc.) live in infra/plugins and cache types in infra/sqlite.
package plugin
